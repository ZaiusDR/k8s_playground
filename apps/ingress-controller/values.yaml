---
traefik:
  ports:
    web:
      nodePort: 30080
    websecure:
      nodePort: 30443
      exposedPort: 443
  service:
    type: NodePort
  certificatesResolvers:
    leresolver:
      acme:
        email: acme@grr.la
        storage: /data/acme.json
        tlsChallenge: {}
